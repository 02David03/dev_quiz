name: dev_quiz
description: DevQuiz - Gabul DEV
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  google_fonts: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_staggered_grid_view: 0.4.0

flutter:
  assets:
    - assets/images/

  uses-material-design: true
